# Pull Request Validation Workflow
#
# Purpose:
# This workflow runs the CI/CD pipeline for pull requests to maintain code quality,
# validate infrastructure changes, and ensure readiness for merging to main branch.
#
# Functionality:
# - Detects changed files in the pull request to determine required validation steps
# - Runs per-tool lint workflows for static code analysis across multiple languages
# - Validates Terraform configurations using init, validate, plan, and test operations
# - Checks Azure IoT Operations component versions against latest published releases
# - Ensures consistent documentation standards
# - Tests resource provider scripts in PowerShell and bash
#
# Parameters:
# None - This workflow is triggered automatically on pull requests to protected branches
#
# Output Variables:
# None - Results are provided as GitHub checks, annotations, and artifacts
#
# Usage Examples:
# This workflow is automatically triggered on pull requests and doesn't need manual invocation.
#
# This comprehensive validation workflow helps maintain code quality and prevents
# problematic code from being merged into the main branch.
---
name: PR Validation

permissions: {}

on:  # yamllint disable-line rule:truthy
  pull_request:
    # Only run on PRs targeting the main branch
    branches:
      - main
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      includeIaCFolders:
        description: 'Include all Infrastructure as Code folders in the matrix check'
        required: true
        default: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  # CodeQL Analysis job for PR code security scanning
  codeql-analysis:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: ['javascript', 'python', 'typescript', 'csharp', 'rust']
        # CodeQL supports these languages: https://aka.ms/codeql-docs/language-support

    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4.38.0
        with:
          languages: ${{ matrix.language }}
          build-mode: none

      - name: Autobuild
        uses: github/codeql-action/autobuild@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4.38.0

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4.38.0
        with:
          category: "/language:${{matrix.language}}"

  # Dependency scanning job for PRs
  dependency-scan:
    name: Dependency Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Dependency Review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          config-file: ./.github/dependency-review-config.yml

  # Shell script linting
  shell-lint:
    name: Shell Lint
    permissions:
      contents: read
    uses: ./.github/workflows/shell-lint.yml
    with:
      soft-fail: false

  # Terraform linting
  terraform-lint:
    name: Terraform Lint
    permissions:
      contents: read
    uses: ./.github/workflows/terraform-lint.yml
    with:
      soft-fail: false

  # Bicep linting
  bicep-lint:
    name: Bicep Lint
    needs: [matrix-changes]
    # changesInBicepInstall already implies full validation: Detect-Folder-Changes.ps1 sets
    # has_changes = true whenever full_validation_required is true, so no separate clause is needed.
    if: github.event_name != 'pull_request' || needs.matrix-changes.outputs.changesInBicepInstall == 'true'
    permissions:
      contents: read
    uses: ./.github/workflows/bicep-lint.yml
    with:
      full-validation: ${{ github.event_name != 'pull_request' || needs.matrix-changes.outputs.bicepFullValidationRequired == 'true' }}
      bicep-folders-json: ${{ needs.matrix-changes.outputs.changedBicepFolders }}

  # Documentation linting
  docs-lint:
    name: Documentation Lint
    permissions:
      contents: read
    uses: ./.github/workflows/docs-lint.yml

  # Code quality checks
  code-quality-lint:
    name: Code Quality Lint
    permissions:
      contents: read
    uses: ./.github/workflows/code-quality-lint.yml
    with:
      soft-fail: false

  # PowerShell linting
  powershell-lint:
    name: PowerShell Lint
    permissions:
      contents: read
    uses: ./.github/workflows/powershell-lint.yml

  # Security scanning
  security-scan:
    name: Security Scan
    permissions:
      contents: read
      security-events: write
    uses: ./.github/workflows/security-scan.yml
    with:
      grype-soft-fail: false

  # Workflow permissions deny-all scan
  permissions-scan:
    name: Workflow Permissions Scan
    permissions:
      contents: read
      security-events: write
    uses: ./.github/workflows/workflow-permissions-scan.yml
    with:
      require-deny-all: true

  # YAML and container linting
  yaml-lint:
    name: YAML Lint
    permissions:
      contents: read
    uses: ./.github/workflows/yaml-lint.yml

  # Documentation automation validation for PRs
  docs-automation:
    name: Documentation Automation
    uses: ./.github/workflows/docs-automation.yml
    permissions:
      contents: write
      pull-requests: write
      actions: read
      issues: write
    with:
      force_regenerate: false
    secrets: inherit

  # Docusaurus build and test validation for PRs
  docusaurus-tests:
    name: Docusaurus Tests
    permissions:
      contents: read
    uses: ./.github/workflows/docusaurus-tests.yml
    with:
      soft-fail: false

  # Call docs-check-terraform for PR validation
  docs-check-terraform:
    name: Terraform Documentation Check
    permissions:
      contents: read
    uses: ./.github/workflows/docs-check-terraform.yml
    with:
      break_build: true
      terraformDocsVersion: 'v0.20.0'
    secrets: inherit

  # Call docs-check-bicep workflow
  docs-check-bicep:
    name: Bicep Documentation Check
    permissions:
      contents: read
    uses: ./.github/workflows/docs-check-bicep.yml
    with:
      break_build: true
    secrets: inherit

  # AIO version checks for PRs
  aio-version-check:
    name: AIO Version Check
    permissions:
      contents: read
    uses: ./.github/workflows/aio-version-checker.yml
    with:
      iac-type: all
      break-build: false
  # Rust clippy lint check for PRs
  rust-clippy:
    name: Rust Clippy Lint Check
    permissions:
      contents: read
    uses: ./.github/workflows/rust-clippy.yml
    with:
      break-build: true
    secrets: inherit

  # Rust unit/integration tests with coverage for PRs
  # Gated on Rust-relevant changes via the shared matrix-changes detection job
  # (changesInRust output) to avoid running the expensive coverage matrix on
  # PRs that touch no Rust crates, manifests, or rust-tests workflow files.
  rust-tests:
    name: Rust Tests
    needs: [matrix-changes]
    if: github.event_name != 'pull_request' || needs.matrix-changes.outputs.changesInRust == 'true'
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/rust-tests.yml
    secrets: inherit

  # Dependency advisory audit (cargo-audit + govulncheck) for PRs
  dep-audit:
    name: Dependency Audit
    permissions:
      contents: read
    uses: ./.github/workflows/dep-audit.yml
    with:
      break-build: true
    secrets: inherit

  # ClusterFuzzLite fuzzing for changed Rust/Python/JS fuzz targets
  fuzz:
    name: Fuzz
    permissions:
      contents: read
      security-events: write
      id-token: write
      actions: read
    uses: ./.github/workflows/fuzz-pr.yml
    secrets: inherit

  # Use reusable workflow to detect detailed changes and create folder matrix
  matrix-changes:
    name: Detect Matrix Changes
    needs: [docs-automation]
    uses: ./.github/workflows/matrix-folder-check.yml
    permissions:
      contents: read
      actions: read
    with:
      displayName: 'Detect component changes for PR validation'
      includeIaCFolders: ${{ github.event_name == 'workflow_dispatch' && inputs.includeIaCFolders || false }}
      includeApplications: false

  # PowerShell Resource Provider Tests for PRs
  pwsh-provider-tests:
    name: PowerShell Provider Tests
    permissions:
      contents: read
    uses: ./.github/workflows/resource-provider-pwsh-tests.yml
    with:
      working-directory: 'src/azure-resource-providers'
      test-results-output: 'PWSH-TEST-RESULTS.xml'
    secrets: inherit

  # Terraform variable compliance check for PRs
  terraform-var-compliance:
    name: Terraform Variable Compliance
    needs: [matrix-changes]
    permissions:
      contents: read
    uses: ./.github/workflows/variable-compliance-terraform.yml
    with:
      break_build: false
    secrets: inherit

  # Terraform tests using matrix for each changed module
  terraform-module-tests:
    name: Terraform Module Tests
    needs: [matrix-changes]
    if: needs.matrix-changes.outputs.changesInTfInstall == 'true' && fromJson(needs.matrix-changes.outputs.changedTfFolders).folderName[0] != null
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.matrix-changes.outputs.changedTfFolders) }}
    permissions:
      contents: read
    uses: ./.github/workflows/cluster-test-terraform.yml
    with:
      working-directory: ${{ matrix.folderName }}
      terraform-version: '1.12.2'
      test-apply: false
    secrets: inherit

  # Application matrix builds for each changed application
  application-matrix-builds:
    name: Application Matrix Builds
    needs: [matrix-changes]
    if: needs.matrix-changes.outputs.changesInApplications == 'true' && fromJson(needs.matrix-changes.outputs.changedApplicationFolders).folderName[0] != null
    uses: ./.github/workflows/application-matrix-builds.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: read
      security-events: write
    with:
      applicationMatrix: ${{ needs.matrix-changes.outputs.changedApplicationFolders }}
      registryName: ${{ vars.REGISTRY_NAME || 'ghcr.io' }}
      buildConfig: |
        {
          "dockerComposeVersion": "2.24.5",
          "dockerBuildArgs": {
            "CARGO_PROFILE_RELEASE_DEBUG": "line-tables-only",
            "CARGO_PROFILE_RELEASE_STRIP": "none"
          }
        }
      securityConfig: |
        {
          "pushImages": false,
          "enableSLSA": false,
          "enableSecurityScan": true,
          "securityThreshold": "high"
        }
    secrets: inherit

  # Pester tests for PowerShell scripts
  pester-tests:
    name: Pester Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4.2.2
        with:
          fetch-depth: 0

      - name: Run Pester Tests
        id: pester
        shell: pwsh
        run: |
          ./scripts/Invoke-Pester.ps1 -CI -ChangedOnly

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pester-test-results
          path: test-results/
          retention-days: 30

  # Fabric ontology credential-free contract and lifecycle qualification
  fabric-ontology-qualification:
    name: Fabric Ontology Qualification
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python 3.12
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Install Python Dependencies
        run: |
          python -m pip install \
            attrs==26.1.0 \
            jsonschema==4.26.0 \
            jsonschema-specifications==2025.9.1 \
            pyyaml==6.0.3 \
            referencing==0.37.0 \
            rpds-py==0.30.0 \
            typing-extensions==4.15.0

      - name: Install yq
        shell: bash
        run: |
          YQ_VERSION="v4.53.6"
          YQ_SHA256="c5f056448f973ae7d39b5401949648a78f2dc1947d6a8eb65be60d5c504b9385"
          YQ_PATH="${RUNNER_TEMP}/yq"
          curl --fail --location --retry 3 \
            --output "${YQ_PATH}" \
            "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64"
          echo "${YQ_SHA256}  ${YQ_PATH}" | sha256sum --check -
          chmod +x "${YQ_PATH}"
          echo "${RUNNER_TEMP}" >>"${GITHUB_PATH}"

      - name: Run Fabric Ontology Qualification
        working-directory: src/000-cloud/033-fabric-ontology
        shell: bash
        run: ./tests/validate-generation.sh

  # Aggregator gate - single required check for branch protection
  pr-validation-gate:
    name: PR Validation Gate
    runs-on: ubuntu-latest
    if: always()
    needs:
      - codeql-analysis
      - dependency-scan
      - shell-lint
      - terraform-lint
      - bicep-lint
      - docs-lint
      - code-quality-lint
      - powershell-lint
      - security-scan
      - yaml-lint
      - docs-automation
      - docusaurus-tests
      - docs-check-terraform
      - docs-check-bicep
      - aio-version-check
      - rust-clippy
      - rust-tests
      - dep-audit
      - fuzz
      - matrix-changes
      - pwsh-provider-tests
      - terraform-var-compliance
      - terraform-module-tests
      - application-matrix-builds
      - pester-tests
      - fabric-ontology-qualification
    permissions:
      contents: read
    steps:
      - name: Verify all required checks passed
        shell: bash
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "::error::One or more required PR validation checks failed or were cancelled."
            exit 1
          fi
          echo "All required PR validation checks passed."
